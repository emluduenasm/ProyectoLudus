# .env.example  (copia como .env y completa)
PORT=3000
JWT_SECRET=pon_un_secreto_largo_aleatorio
# Si usás Postgres en Codespaces (servicio local del devcontainer):
DATABASE_URL=postgres://postgres:postgres@postgres:5432/appdb

# Si te vas a conectar a un Postgres externo:
# DATABASE_URL=postgres://usuario:password@HOST:PUERTO/nombre_db
